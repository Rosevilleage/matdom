# Find Mat - 음식 추천 모바일 앱

Feature-Sliced Design (FSD) 아키텍처를 기반으로 구성된 음식 추천 모바일 웹 애플리케이션입니다.

## 📁 프로젝트 구조 (FSD)

```
src/
├── app/                    # 앱 초기화 및 전역 설정
│   ├── index.tsx          # 메인 App 컴포넌트
│   ├── main.tsx           # 앱 진입점
│   └── index.css          # 전역 스타일
│
├── pages/                  # 페이지 레이어
│   ├── home/              # 홈 화면
│   │   ├── ui/
│   │   │   └── HomeScreen.tsx
│   │   └── index.ts
│   └── map/               # 지도 화면
│       ├── ui/
│       │   └── MapScreen.tsx
│       └── index.ts
│
├── widgets/                # 위젯 레이어 (복합 UI 블록)
│   ├── slot-machine/      # 슬롯머신 위젯
│   │   ├── ui/
│   │   │   ├── SlotMachine.tsx
│   │   │   └── SlotMachineIcon.tsx
│   │   └── index.ts
│   ├── food-result-modal/ # 음식 결과 모달
│   │   ├── ui/
│   │   │   └── FoodResultModal.tsx
│   │   └── index.ts
│   └── restaurant-detail/ # 음식점 상세 정보
│       ├── ui/
│       │   └── RestaurantDetail.tsx
│       └── index.ts
│
├── features/               # 기능 레이어
│   ├── search-restaurant/ # 음식점 검색
│   │   ├── ui/
│   │   │   └── SearchBar.tsx
│   │   └── index.ts
│   └── select-category/   # 카테고리 선택
│       ├── ui/
│       │   └── CategoryChips.tsx
│       └── index.ts
│
├── entities/               # 엔티티 레이어 (비즈니스 엔티티)
│   ├── restaurant/        # 음식점 엔티티
│   │   ├── model/
│   │   │   └── types.ts
│   │   ├── ui/
│   │   │   └── RestaurantCard.tsx
│   │   └── index.ts
│   └── food/              # 음식 엔티티
│       ├── ui/
│       │   └── FoodIcons.tsx
│       └── index.ts
│
└── shared/                 # 공유 레이어
    ├── ui/                # 공유 UI 컴포넌트
    │   ├── kit/           # UI 킷 (디자인 시스템)
    │   │   ├── button/
    │   │   ├── badge/
    │   │   ├── card/
    │   │   ├── input/
    │   │   └── ...
    │   ├── toast/         # 토스트 알림
    │   └── map-view/      # 지도 뷰
    ├── lib/               # 유틸리티 함수
    │   └── utils.ts
    └── assets/            # 정적 자산
        └── icons/         # SVG 아이콘
```

## 🏗️ FSD 아키텍처 레이어

### 1. **App Layer** (`app/`)
- 애플리케이션 초기화
- 전역 설정 및 라우팅
- 최상위 컴포넌트

### 2. **Pages Layer** (`pages/`)
- 전체 페이지 컴포넌트
- 라우트별 화면 구성
- 위젯과 피처를 조합

### 3. **Widgets Layer** (`widgets/`)
- 독립적인 복합 UI 블록
- 여러 피처와 엔티티를 조합
- 재사용 가능한 큰 단위의 컴포넌트

### 4. **Features Layer** (`features/`)
- 사용자 시나리오와 기능
- 비즈니스 로직을 포함한 UI
- 엔티티를 사용하여 기능 구현

### 5. **Entities Layer** (`entities/`)
- 비즈니스 엔티티
- 도메인 모델과 타입
- 엔티티 관련 UI 컴포넌트

### 6. **Shared Layer** (`shared/`)
- 공통 유틸리티
- UI 킷 (디자인 시스템)
- 재사용 가능한 헬퍼 함수

## 🚀 시작하기

### 설치
```bash
npm install
# 또는
pnpm install
```

### 개발 서버 실행
```bash
npm run dev
# 또는
pnpm dev
```

### 빌드
```bash
npm run build
# 또는
pnpm build
```

## 📦 주요 의존성

- **React** - UI 라이브러리
- **TypeScript** - 타입 안정성
- **Vite** - 빌드 도구
- **Tailwind CSS** - 스타일링
- **Framer Motion** - 애니메이션
- **@tabler/icons-react** - 아이콘
- **class-variance-authority** - 컴포넌트 variants 관리

## 🎯 주요 기능

1. **음식 슬롯머신** - 랜덤 음식 추천
2. **지도 기반 검색** - 주변 음식점 찾기
3. **카테고리 필터** - 음식 종류별 필터링
4. **음식점 상세 정보** - 평점, 거리, 영업시간 등

## 📝 Import 규칙

FSD 아키텍처에서는 다음과 같은 import 규칙을 따릅니다:

```typescript
// ✅ 올바른 import
import { Restaurant } from '@/entities/restaurant';
import { SearchBar } from '@/features/search-restaurant';
import { SlotMachine } from '@/widgets/slot-machine';
import { Button } from '@/shared/ui/kit';

// ❌ 잘못된 import (내부 구현 직접 접근)
import { Restaurant } from '@/entities/restaurant/model/types';
import { SearchBar } from '@/features/search-restaurant/ui/SearchBar';
```

각 레이어는 `index.ts`를 통해 public API를 노출합니다.

## 🔄 의존성 규칙

FSD에서는 상위 레이어만 하위 레이어를 import할 수 있습니다:

```
app → pages → widgets → features → entities → shared
```

- `pages`는 `widgets`, `features`, `entities`, `shared`를 사용 가능
- `widgets`는 `features`, `entities`, `shared`를 사용 가능
- `features`는 `entities`, `shared`를 사용 가능
- `entities`는 `shared`만 사용 가능
- `shared`는 다른 레이어를 import하지 않음

## 📄 라이선스

MIT
